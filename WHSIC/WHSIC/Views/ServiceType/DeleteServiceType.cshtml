@model WHSIC.Model.ServiceTypeViewModel

@{
    ViewBag.Title = "DeleteServiceType";
    Layout = "~/Views/Treasure/_DashBoard.cshtml";
}

<h2 class="text-center">Delete Service Type</h2>
<div class="well">
    <h3>Are you sure you want to delete this?</h3>
    <div>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.Description)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Description)
            </dd>

            <dt>
                In high demand
            </dt>

            <dd>
                @Html.DisplayFor(model => model.dismiss)
            </dd>

        </dl>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color">
                <input type="submit" value="Delete" class="btn btn-primary" /> |
                @Html.ActionLink("Back to List", "GetAllServiceType")
            </div>
        }
    </div>
</div>