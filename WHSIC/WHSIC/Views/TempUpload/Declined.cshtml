@model WHSIC.Model.TempUploadViewModel

@{
    ViewBag.Title = "Declined";
    Layout = "~/Views/Admin/_DashBoard.cshtml";
}

<h2>Declined</h2>

@using (Html.BeginForm("Declined","TempUpload",FormMethod.Post)) 
{
    @Html.AntiForgeryToken()
            string id =(string) ViewBag.c;
    <div class="form-horizontal">
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">

                
                <textarea class="form-control" placeholder="Please provide reason for declining" name="comment" rows="10" cols="25"></textarea>
                </div>
            </div>
                <br />
                <div class="form-group">
                    <div class="col-md-offset-2 col-md-10">
                        <input type="submit" value="Proceed" class="btn btn-default"style = "background-color:purple; color:gold;opacity:0.6" />
                    </div>
                </div>
            </div>
            }

            <div>
                @Html.ActionLink("Back to List", "Index", "TempUpload")
            </div>
